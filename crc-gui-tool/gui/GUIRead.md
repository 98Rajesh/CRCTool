V0.0.0
What This Gives You Immediately
    ✔ Multi-panel professional layout
    ✔ Supports file + raw input
    ✔ Full CRC parameter configurability
    ✔ Log window (error/info/debug)
    ✔ Hooks ready for:
        Intel HEX / Motorola S-Record
        Table / non-table CRC
        Code generation (C/Python)
        Plugin-style CRC standards

Strong Next Enhancements (Recommended)
    CRC profile loader (JSON for standards)
    HEX parser auto-detect (Intel vs Motorola)
    Bit-wise preview (input stream visualization)
    Test vectors panel
    Export result report (TXT / JSON)
    CLI + GUI share same CRC engine
    Dark/light theme switch